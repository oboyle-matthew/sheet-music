import React from 'react';
import styled from "styled-components";

export const GenericFlag = styled.img`
    position: absolute;
    // height: ${props => props.stemHeight}px;
    // // background: yellow;
    // top: ${props => props.topOffset}px;
    // left: ${props => props.leftOffset}px;
    height: 100px;
    top: 0;
    left: 0;
    z-index: 80;
`;